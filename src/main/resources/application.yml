server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/postgres
    username: user
    password: password
  jpa:
    show-sql: true
    open-in-view: false
  liquibase:
    change-log: classpath:liquibase/changelog-master.yml